in your django app, add a new file called url.py. (name file does not matter)

in the file, you can add the following lines of code as an example.

from django.urls import path
from . import views

urlpatterns = [


]
